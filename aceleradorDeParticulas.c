#include <stdio.h>

int main(int argc, char const *argv[])
{
  int distancia, voltas, distaciatotal;
  distacia = distancia - 3;

  voltas = floor(distancia/8);

  distaciatotal = voltas*8;

  if(distaciatotal +5 <= distancia){
    print(3)
  }else if (distaciatotal +4 <= distancia){
    print(2)
  }else{
    print(distancia)
    print(1)
  }
  return 0;
}
